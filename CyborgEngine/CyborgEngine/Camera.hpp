/*#ifndef CAMERA_HPP
#define CAMERA_HPP

void computeMatrices(); //reads the keyboard and mouse and computes the Projection and View matrices.
glm::mat4 getViewMatrix(); // returns the computed Projection matrix.
glm::mat4 getProjectionMatrix(); //returns the computed View matrix.
 
#endif*/